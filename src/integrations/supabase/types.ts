// Decommissioned by S0.2.
//
// Generated Supabase database types. They described a schema with zero tables, zero
// views and zero functions, and were only ever referenced by ./client.ts. Emptied
// rather than deleted because deleting files is Angad's call — this file can go.
export {};
